{
  "name": "smartwallet-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npm run dev:frontend & npm run dev:backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "db:migrate": "wrangler d1 migrations apply DB --local -c backend/wrangler.jsonc",
    "db:generate": "wrangler d1 migrations create DB new_migration -c backend/wrangler.jsonc"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
